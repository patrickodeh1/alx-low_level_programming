 #include "main.h"

/**
 * _islower - check if character is lowercase
 * @c: int value
 * Return: 1 if true and 0 if false
 */

int _islower(int c)
{
	if (c >= 97 && c <= 122)
		return (1);
	else
		return (0);
	return (0);
}
