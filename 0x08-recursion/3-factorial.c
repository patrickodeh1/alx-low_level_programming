#include "main.h"

/**
 * factorial - returns factorial of a number
 * @n: number
 * Return: factorial
 */

int factorial(int n)
{
	if (n < 0)
		return (-1);
	else if (n == 0)
		return (n);
	else
		return (1 + factorial(s + 1));
}
